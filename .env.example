# Optional. Without a key Kairos runs in Local Mind mode (deterministic planner, no LLM).
ANTHROPIC_API_KEY=
# Default model when a key is present.
KAIROS_MODEL=claude-opus-5
# Where the SQLite database lives.
KAIROS_DB=./data/kairos.db
PORT=8787
